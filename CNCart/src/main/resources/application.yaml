spring:
  application:
    name: CNCart
  datasource:
    url: jdbc:mysql://localhost:3306/CNKart?createDatabaseIfNotExist=true
    username: root
    password: Navyug@123
  jpa:
    properties.hibernate-dialect = org.hibernate.dialect.MySQLDialect
    hibernate.ddl-auto = update